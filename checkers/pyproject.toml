[tool.poetry]
name = "checkers"
version = "0.1.0"
description = ""
authors = ["jmrflora <joseribeiroflora@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pygame = "^2.6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
